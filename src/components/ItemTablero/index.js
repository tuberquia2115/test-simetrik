import React from 'react'
import { Container } from './styles'

export const ItemTablero = ({
    isActive,
    dashboardName,
    visualType,
    visuals,
    timestamp,
    description,
    tags

}) => {
    return (
        <Container>
            <p>dashboardName</p>
        </Container>
    )
}
