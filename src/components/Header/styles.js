import styled from "@emotion/styled";

export const StyledHeader = styled.nav`
  background-image: linear-gradient(to right, var(--primary-dark), var(--secodary-dark));
  color: #ffffff;
  padding: 0.5rem;
  text-align: center;
  font-size: 30px;
`;
